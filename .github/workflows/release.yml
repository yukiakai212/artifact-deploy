name: Release and Publish

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: yukiakai212/actions-public/.github/workflows/publish-action-release.yml@v1

    secrets: inherit